const fn = () => {
    const miString = "Soy un string"
    let b = `${miString} very happy`
    console.log(b)
    clg
}

fn()

const clickeado = () => {
    let a = "Texto de ejemplo"

    console.log(a)
}